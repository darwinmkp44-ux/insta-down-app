package com.instadown.app.ui.theme

import androidx.compose.ui.graphics.Color

// Canvas & Surfaces
val BackgroundCanvas = Color(0xFF070412)       // Deep cosmic black/indigo
val GlassBase = Color(0x3D1A1635)               // Semi-transparent deep indigo (24% alpha)
val GlassBorder = Color(0x52FFFFFF)             // Subtle white highlight for glass border (32% alpha)
val CardBackground = Color(0xFF140F2D)          // Non-transparent card fallback

// Neon accents & Brand gradients
val NeonPink = Color(0xFFFF007A)               // Electric magenta
val NeonViolet = Color(0xFF8A00FF)             // Electric purple
val NeonCyan = Color(0xFF00F0FF)               // Glowing cyan for highlights
val GlowAlert = Color(0xFFFF3333)               // Glowing red for warnings/errors
val GlowSuccess = Color(0xFF00FF85)             // Glowing neon green for completed downloads

// Typography & Icons
val TextPrimary = Color(0xFFFFFFFF)             // High-contrast pure white
val TextSecondary = Color(0xFFB3AFD2)           // Soft purple-gray for secondary text
val TextMuted = Color(0xFF6B658A)               // Deep purple-gray for hints
